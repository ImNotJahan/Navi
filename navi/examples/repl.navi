| 
Creates a read-eval-print loop
Demonstrates (listen) & (reflect)
|

(set (repl) 
	(let ((input (listen)))
		(when (not= input null) do ~ remember that empty strings are equal to null
			(sayline (reflect input))
			(repl))))

(repl)